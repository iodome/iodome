{
  "name": "iodome-dev",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.29.4"
  }
}
